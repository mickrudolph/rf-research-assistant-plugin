{
  "name": "rf-google-drive-mcp",
  "version": "1.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "ncc build server.js -o dist/server && ncc build auth.js -o dist/auth && cd dist/server && ln -sf ../auth/index.js auth.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "google-auth-library": "^9.15.1",
    "googleapis": "^148.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}

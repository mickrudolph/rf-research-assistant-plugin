{
  "name": "rf-coda-mcp",
  "version": "1.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "ncc build launcher.js -o dist/launcher && ncc build auth.js -o dist/auth"
  },
  "dependencies": {},
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
